# 42call_me_maybe
A Python tool that translates natural language prompts into structured function calls using a small LLM (Qwen3-0.6B), powered by constrained decoding to guarantee 100% valid JSON output regardless of model size.
